{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Vision Flow",
            "type": "debugpy",
            "request": "launch",
            "module": "visionflow",
            "console": "integratedTerminal",
            "justMyCode": true,
            "cwd": "${workspaceFolder}",
            "python": "${command:python.interpreterPath}"
        }
    ]
}